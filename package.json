{
  "name": "monorepo-usuarios",
  "version": "1.0.0",
  "scripts": {
    "api": "npm run dev --prefix api",
    "frontend": "npm start --prefix frontend",
    "dev": "concurrently \"npm run api\" \"npm run frontend\""
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.6.0",
    "concurrently": "^9.2.1"
  }
}
